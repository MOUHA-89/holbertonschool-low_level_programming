#include <stdio.h>
#include "main.h"
/**
 * main - main block
 * @argc: number of arguments
 * @argv: is a arrea string
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);

return (0);
}
