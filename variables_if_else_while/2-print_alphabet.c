#include <stdio.h>
/**
 * main - main block
 * Return: 0
 */
int main(void)
{
boucle [a - z] is alphabet
char alphabet = 'a'
while alphabet <= 'z'
{
putchar(alphabet);
alphabet++;
}
putchar('\n');
return (0);
}
