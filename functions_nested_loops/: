#include "main.h"
/**
 * _islower - checks for lowercase character
 * Return: 0
 */
int _islower(int c)
{
int i;
char islower = 'a';
for (; islower <= 'z'; k++)
if (i == 1)
{
_putchar(islower);
}
else
{
return (0);
}
